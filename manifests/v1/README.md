# Frozen manifest output

`scripts.freeze_split` writes the final public `partitions.parquet` here only after raster quality, footprint-buffer, support, and evaluation-block checks. Generated Parquet and normalization JSON artifacts are ignored; this README records their contract.
