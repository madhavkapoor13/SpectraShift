# Week 3 generated evidence

The Kaggle pilot writes `week3_run_summary.json`, per-run `ssl_summary.json`, `probe_summary.json`, JSONL training logs, resumable epoch checkpoints, and `runs.jsonl`. Preserve the output as the private Kaggle dataset `spectrashift-week3-pilots` and download the small summaries for repository records. Checkpoints remain outside Git.
