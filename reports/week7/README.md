# Week 7 artifacts

Week 7 adds DINOv2 ViT-S/14 and OlmoEarth v1.1 Tiny as external foundation-model context. The implementation includes pinned asset contracts, bounded pilots, full fine-tuning anchors, frozen feature caches, linear and k-NN probes, validated recovery, and a Week 8 approval aggregate.

Large model assets, checkpoints, feature caches, logits, and notebook outputs remain in private Kaggle datasets. After successful execution, preserve only the compact aggregate files under `generated/`.
