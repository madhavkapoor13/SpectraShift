# Week 9 artifacts

Week 9 is implemented for private Kaggle execution. It completes the frozen M1-M6 representation-probe matrix and analyzes the six existing 10% M3/M4 checkpoints without updating an encoder or changing model selection.

After successful aggregation, copy only the compact tables, figures, and `week9_run_summary.json` into `reports/week9/generated/`. Evaluation labels, logits, checkpoint files, cached patch features, nearest-neighbour patch records, and notebook outputs remain in private Kaggle datasets or ignored paths.
