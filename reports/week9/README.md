# Week 9 artifacts

Week 9 is complete. It contains the frozen M1-M6 representation-probe matrix and analyses of the six existing 10% M3/M4 checkpoints without updating an encoder or changing model selection.

Only compact tables, figures, and `week9_run_summary.json` are tracked under `generated/`. Evaluation labels, logits, checkpoint files, cached patch features, nearest-neighbour patch records, spectral-sensitivity patch records, and notebook outputs remain in private Kaggle datasets or ignored paths.

The aggregate passed all completion gates: 108 linear probes, 18 k-NN probes, six diagnostic checkpoints, 108 stress prediction-domain states, 1,000 CKA patches, and 3,000 neighbour rows. See `summary.md` for the result interpretation.
