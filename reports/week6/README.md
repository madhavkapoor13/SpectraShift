# Week 6 artifacts

Week 6 completes the six-point M0-M4 label-efficiency curves and adds the matched three-seed ImageNet RGB control.

Large checkpoints, V logits, staged arrays, and notebook outputs remain in private Kaggle datasets. This directory tracks the implementation summary and exact execution procedure. After the CPU aggregate passes, copy the compact `week6_run_summary.json`, `controlled_curves.csv`, `paired_differences.csv`, and `aulc_summary.csv` here for the permanent repository record.
