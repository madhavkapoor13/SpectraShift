# Week 6 artifacts

Week 6 completes the six-point M0-M4 label-efficiency curves and adds the matched three-seed ImageNet RGB control.

Large checkpoints, V logits, staged arrays, and notebook outputs remain in private Kaggle datasets. The verified compact aggregate artifacts are preserved under `generated/`: `week6_run_summary.json`, `controlled_curves.csv`, `paired_differences.csv`, and `aulc_summary.csv`.
