# Week 8 artifacts

Week 8 is the one-time frozen evaluation on I, Finland, and Portugal. The implementation verifies the exact Week 7 approval and 111-checkpoint ledger before opening labels, keeps labels separate from prediction files, evaluates every registered checkpoint without parameter updates, and performs paired geographic uncertainty analysis.

Evaluation labels, logits, checkpoints, and notebook outputs remain in private Kaggle datasets and ignored paths. After successful execution, preserve only the compact aggregate tables under `generated/`.
